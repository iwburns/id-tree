sudo: false
language: rust
cache: cargo

addons:
  apt:
    packages:
      - libcurl4-openssl-dev
      - libelf-dev
      - libdw-dev
      - binutils-dev
      - cmake
    sources:
      - kalakris-cmake

rust:
  - stable
  - beta
  - nightly
matrix:
  allow_failures:
    - rust: nightly

before_script:
  - export PATH=$HOME/.cargo/bin:$PATH
  - which cargo-travis || cargo install cargo-travis
  - which rustfmt || cargo install rustfmt

script:
  - |
      cargo fmt -- --write-mode=diff &&
      cargo build &&
      cargo test &&
      cargo bench &&
      cargo doc

after_success:
  # measure code coverage and upload to coveralls.io
  - cargo coveralls
