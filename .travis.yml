language: rust
cache: cargo
rust:
  - stable
  - beta
  - nightly
matrix:
  allow_failures:
    - rust: nightly
before_script: (cargo install rustfmt || true)
script: cargo fmt -- --write-mode=diff && cargo build --verbose && cargo test --verbose